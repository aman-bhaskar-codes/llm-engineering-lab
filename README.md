# Structured Extraction Engine

A robust backend engine for structuring text using Large Language Models like OpenAI. Built with FastAPI.

## Usage
Run the application using:
```bash
uv run uvicorn app.main:app --reload
```
