# To connect your deployed Next.js frontend to your Cloud Run backend API:
# Replace the URL below with the one generated after running `./deploy.sh`
NEXT_PUBLIC_API_BASE_URL=https://extraction-api-something.run.app/api/v1
